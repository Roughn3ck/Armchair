# Agent In The Armchair — Voice-to-Text + AI Agent for MS Teams
# Python dependencies for WSL pipeline (Python 3.12+)
#
# Install: pip install -r requirements.txt
# Note: faster-whisper requires CUDA toolkit for GPU acceleration
# Note: pyannote-audio requires HuggingFace token (see README)

# Speech-to-Text
faster-whisper>=1.1.0

# Speaker Diarization
pyannote-audio>=4.0.0

# TTS (Piper — CPU-only, fast, standard voices)
piper-tts>=1.2.0
onnxruntime>=1.16.0

# Audio processing
numpy>=1.24.0
soundfile>=0.12.0

# HTTP server (dashboard)
# (stdlib only — no extra deps)

# System
requests>=2.28.0