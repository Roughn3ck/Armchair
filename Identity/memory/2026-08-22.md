# 2026-08-22 — v2 Website Build

## What happened
Kris asked to push the TCN website forward ahead of the Monday meeting: build a v2 of the static/self-hosted site to "Wix-ify" it — more responsive widths, better layout, smoother nav.

## Key design direction (from Kris)
- **Keep the blue background**, but use **white blocks** for content (the reverse of Alan's PowerPoint style — Alan does reverse, Kris likes white bg with colour blocks).
- Regenerate background/watermark images for all content blocks.
- Keep the "phenomenal watermark" theme going — the first two blocks ("When the waters go dark..." + "Two months offline") had great watermark imagery; extend that.

## What was done
1. **Archived v1** → `tcn-preview-v1/` (full copy of the original `tcn-preview/`).
2. **Regenerated 4 watermark images** (v2):
   - `hero-lighthouse-v2.jpg` — lighthouse on rocky headland, beam over stormy ocean
   - `chart-course-v2.jpg` — nautical chart with plotted course + compass rose
   - `three-domains-v2.jpg` — three interlocking translucent rings (physical/personnel/cyber)
   - `compass-emblem-v2.jpg` — brass compass emblem
   - Copied into `tcn-preview/public/img/`
3. **Built v2 `index.html`** with:
   - Blue (deep navy) background retained, with fixed chart-grid + chart-lines texture layers
   - White content cards (`.card`) floating on the blue, each with a watermark image (`.wm`) at ~10% opacity behind the content
   - Full-bleed hero with lighthouse watermark at higher opacity
   - Responsive grid (3→1 domains, 2→1 services/about) with breakpoint at 900px
   - Sticky nav with blur + mobile hamburger toggle
   - Smoother hover transitions, softer shadows, larger radii (Wix-like polish)
   - Gold-accented governance card (badge + 2px gold border)

## Verified
- Served locally via `python3 -m http.server`, opened in browser, screenshot confirmed:
  - Hero renders (navy + lighthouse watermark + white headline)
  - White "domains" card renders correctly with 3 sub-cards + subtle watermark

## Status
- v2 built and verified. Ready to share with Kris/Alan.
- v1 safely archived at `tcn-preview-v1/`.
- The images are in the managed media dir AND copied into the preview img dir (dual safety).

## Next (for Monday)
- Share v2 screenshot + live preview with Alan
- Get Alan's reaction to white-blocks-on-blue vs his reverse preference
- Discuss whether to push to Cloudflare preview or hold
- Still pending: compliance pages (privacy/cookie/anti-bribery), platform decision (Wix vs Cloudflare), domain migration question
